GET choice from user.
Create function getHumanChoice().
    GET a string from user.
    Return string.
GET choice from computer.
Create function getComputerChoice().
    GET a float bigger than or equal to 0 and less than 1.
    MULTIPLY float by three to get new number between 0 and 3 (latter excluded).
    IF number is less than 3 and bigger than or equal to 2 THEN
        Return "rock".
    ELSE IF number is less than 2 and bigger than or equal to 1 THEN
        Return "paper".
    ELSE
        Return "scissors".
    ENDIF
SET players' score to 0.
Create function playRound().
    COMPARE choices from computer and user.
    IF choices are equal THEN
        Return "It's a draw! Go again.".
    ELSE
        IF humanChoice is "rock" and computerChoice is "paper"
        OR humanChoice is "paper" and computerChoice is "scissors"
        OR humanChoice is "scissors" and computerChoice is "rock" THEN
            ADD 1 to computerChoice's score.
            Return "You lose! computerChoice beats humanChoice.".
        ELSE
            ADD 1 to humanChoice's score.
            Return "You win! humanChoice beats computerChoice.".
        ENDIF
    ENDIF